<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssg.springboard.mappers.ReplyMapper">

    <insert id="insert">
        <selectKey order="AFTER" keyProperty="rno" resultType="long">
            SELECT LAST_INSERT_ID()
        </selectKey>
        INSERT INTO reply (bno, replyText, replyer)
        VALUES (#{bno}, #{replyText}, #{replyer})
    </insert>

    <select id="selectOne" resultType="ReplyVO">
        SELECT * FROM reply
        WHERE rno = ${rno}
    </select>

    <update id="updateOne">
        UPDATE reply SET
        replyText = #{replyText},
        updateDate = now()
        WHERE rno = #{rno}
    </update>

    <delete id="deleteOne">
        DELETE FROM reply
        WHERE rno = #{rno}
    </delete>

    <select id="getReplyList" resultType="ReplyVO">
        SELECT * FROM reply
        WHERE bno = #{bno} and rno > 0
        limit #{cri.skip}, #{cri.amount}
    </select>

</mapper>